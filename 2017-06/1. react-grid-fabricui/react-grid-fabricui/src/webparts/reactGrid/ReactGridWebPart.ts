import * as React from 'react';
import * as ReactDom from 'react-dom';
import { Version } from '@microsoft/sp-core-library';
import {
  BaseClientSideWebPart,
  IPropertyPaneConfiguration,
  PropertyPaneTextField
} from '@microsoft/sp-webpart-base';

import * as strings from 'reactGridStrings';
import ReactGrid from './components/ReactGrid';
import { IReactGridProps } from './components/IReactGridProps';
import { IReactGridWebPartProps } from './IReactGridWebPartProps';

export default class ReactGridWebPart extends BaseClientSideWebPart<IReactGridWebPartProps> {

  public render(): void {
    const element: React.ReactElement<IReactGridProps > = React.createElement(
      ReactGrid,
      {
        description: this.properties.description
      }
    );

    ReactDom.render(element, this.domElement);
  }

  protected get dataVersion(): Version {
    return Version.parse('1.0');
  }

  protected getPropertyPaneConfiguration(): IPropertyPaneConfiguration {
    return {
      pages: [
        {
          header: {
            description: strings.PropertyPaneDescription
          },
          groups: [
            {
              groupName: strings.BasicGroupName,
              groupFields: [
                PropertyPaneTextField('description', {
                  label: strings.DescriptionFieldLabel
                })
              ]
            }
          ]
        }
      ]
    };
  }
}
